package com.yukthitech.validation;

/**
 * Base interface for types which needs to support validators, that support validation on one of its sub fields.
 */
public interface IStringConvertible
{
	public String toStringValue();
}
